version: "3.9"

services:
  doc-ai-rag:
    build: .
    container_name: doc-ai-rag
    ports:
      - "8501:8501"
    volumes:
      # Persist ChromaDB data between container restarts
      - ./data/chroma_db:/app/data/chroma_db
      - ./data/uploads:/app/data/uploads
    environment:
      # Point to host Ollama — adjust if Ollama runs in a different container
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - OLLAMA_MODEL=llama3
      - EMBEDDING_MODEL=all-MiniLM-L6-v2
      - CHUNK_SIZE=1000
      - CHUNK_OVERLAP=200
      - TOP_K=5
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

# Optional: run Ollama in a container too
# ollama:
#   image: ollama/ollama:latest
#   container_name: ollama
#   ports:
#     - "11434:11434"
#   volumes:
#     - ollama_data:/root/.ollama
#   restart: unless-stopped

# volumes:
#   ollama_data:
